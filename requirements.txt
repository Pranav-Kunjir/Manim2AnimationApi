# Core dependencies
manim==0.18.1        # Manim library for animations
numpy>=1.23.0        # Required by manim
Pillow>=9.5.0        # Image handling for manim
cairo>=1.23.0        # Rendering backend for manim (pycairo)
pycairo>=1.23.0

# If using LaTeX (optional, for MathTex rendering) 
# you’ll also need a TeX distribution installed on the system (not via pip)

# API dependencies
fastapi==0.115.0     # Latest stable FastAPI
uvicorn[standard]==0.30.6  # ASGI server for FastAPI
pydantic>=2.9        # Data validation

# Optional helpers
python-multipart>=0.0.7   # If you plan to accept file uploads